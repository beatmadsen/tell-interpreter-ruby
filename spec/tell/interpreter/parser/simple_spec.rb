require 'spec_helper'

RSpec.describe Tell::Interpreter::Parser::Simple do
  context 'with single token input' do
    let(:parser) { build(:parser) }

    it 'complains about missing main class' do
      raise 'not implementated'
    end
  end
end
